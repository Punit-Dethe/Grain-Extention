# Hello (com.example.hello)

Store copy for the example submission. This is the long description shown on the
extension's store page and public site.

**What it does.** A minimal scripted extension that demonstrates the transcript
transform hook — it is the worked example used to prove the registry end to end.

**Capabilities requested.** `transform:transcript` only.
