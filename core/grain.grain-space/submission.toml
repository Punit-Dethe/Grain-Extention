# Grain Space — a first-party BUILTIN extension (`core` trust). Identity, store
# listing and settings schema ship as a normal pack; the implementation is
# compiled into Grain, because the feature owns an ONNX embedding engine,
# sqlite-vec, a native overlay window and global shortcuts. Installing it turns
# the feature on; uninstalling turns it off and never touches your notes.

id = "grain.grain-space"
source_repo = "https://github.com/Punit-Dethe/Grain"
tag = "v1.0.1"
commit = "0000000000000000000000000000000000000000"
summary = "Speak a note, ask a question, get an answer from your own notes — as plain Markdown, fully local."
categories = ["tools"]
license = "MIT"
contact = "@Punit-Dethe"
