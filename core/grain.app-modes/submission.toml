# App Modes — a first-party scripted extension built from the Grain repo by our
# own CI (`core` trust). The externalised form of Context Awareness's modes, and
# the reference example for the transform hook + capture:app + llm.

id = "grain.app-modes"
source_repo = "https://github.com/Punit-Dethe/Grain-Extention"
tag = "v1.0.0"
commit = "0000000000000000000000000000000000000000"
summary = "Per-app formatting modes: when the focused app matches, reformat the transcript with your prompt."
categories = ["productivity"]
license = "MIT"
contact = "@Punit-Dethe"
